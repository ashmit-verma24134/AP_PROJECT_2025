package edu.univ.erp.ui.student;

/** Simple callback invoked after a successful registration/drop that should refresh UI. */
public interface RegistrationListener {
    void onRegistrationChanged();
}
